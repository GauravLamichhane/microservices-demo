services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.local # or Dockerfile.local
    ports:
      - "80:80"
    networks:
      - microservices-net
      - default

networks:
  microservices-net:
    external: true
