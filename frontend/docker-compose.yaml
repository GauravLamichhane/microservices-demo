services:
  frontend:
    image: ghcr.io/GauravLamichhane/microservices-demo/frontend:latest
    mem_limit: 20m
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - microservices-net
      - default

networks:
  microservices-net:
    external: true
